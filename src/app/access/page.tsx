"use client";
import {useState}from"react";
import {getSession}from"../../lib/firebase-rest";
import {redeemAllAccess}from"../../lib/billing-cloud";
export default function Access(){const[code,setCode]=useState(""),[note,setNote]=useState(""),[busy,setBusy]=useState(false);const signed=!!getSession();async function redeem(){setBusy(true);setNote("");try{const r=await redeemAllAccess(code);setNote(r.message);setCode("")}catch(e){setNote(e instanceof Error?e.message:"Unable to redeem code.")}finally{setBusy(false)}}return <main className="shell"><section className="panel legal"><h1>CactusByte ID™ All Access</h1><p>Redeem an authorized CactusByte All-Access code to activate Pro access across every paid Cactus🌵Byte Studios™ app tied to this CactusByte ID™, including future paid apps that honor CactusByte entitlements.</p>{!signed?<p><b>Sign in from the Command Center first.</b></p>:<div className="form"><input value={code} onChange={e=>setCode(e.target.value.toUpperCase())} placeholder="Enter All-Access code" autoCapitalize="characters" autoCorrect="off"/><button disabled={busy||!code.trim()} onClick={()=>void redeem()}>{busy?"Activating…":"Activate All Access Pro"}</button></div>}{note&&<div className="notice">{note}</div>}<p><a href="/billing">Billing &amp; Access Center™</a> · <a href="/">Return to Cactus🌵Byte Studios™</a></p></section><footer>Cactus🌵Byte Studios™ v1.3.0 · Cactus🌵Byte Studios™ · All Rights Reserved</footer></main>}
