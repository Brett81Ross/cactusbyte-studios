name: Acelynn Pro Phase 8 Emulator Gate v2 (Superseded)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  superseded:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain authoritative Acelynn transition proof
        run: |
          cat <<'EOF'
          This redundant Phase 8 v2 emulator workflow is retired and manual-only.

          Do not use its historical red run as release authority. The authoritative
          Acelynn Pro signing-transition proof is GitHub Actions run 33869737869,
          which used the literal August 31 distributed legacy APK and the certified
          permanent-signed Acelynn Direct APK on API 36 and completed the four-
          snapshot export -> emulator-only uninstall -> permanent install -> restore
          sequence successfully.

          Evidence authority:
            android-packager/ACELYNN_SIGNING_TRANSITION_EVIDENCE.md
            transition run: 33869737869
            transition evidence artifact: 9935674326
            certified signed APK artifact: 9850425582
            permanent Acelynn signer SHA-256:
            FA956BB761F59AAD38FAEF4F4F561FBF8EB05C1A7CB6DD0E30C12D52CC153230

          This workflow intentionally performs no emulator boot, build, signing,
          deployment, publication, main merge, or physical-device cutover.
          EOF
